# 开发日志：2026-07-12

## 当日目标

- 待填写。

## 已完成事项

- 待填写。

## 测试及验证

- 待填写。

## 技术决策

- 无。

## 风险、问题和阻塞

- 无。

## 未完成事项

- 待填写。

## 下一开发日待办

- 待填写。

<!-- AUTO-SUMMARY:START -->
## 自动汇总

- 汇总时间：2026-07-12 00:44:16 +08:00
- 脚本版本：1.0.0

### 当日 Git 提交

- 7db57c9 完善 XMBC 扩展标记语义
- 5432486 完成基础宏导入导出链路
- c356450 修复干净仓库日报误报
- 749b2be 支持 Synapse 4 整包只读导入

### 当前工作区变更

- M XMacroBridge.sln
- M devlogs/2026/2026-07-12.md
- M "docs/02-\347\263\273\347\273\237\346\236\266\346\236\204\350\256\276\350\256\241.md"
- M "docs/03-\345\256\217\346\240\274\345\274\217\350\247\204\350\214\203.md"
- M "docs/06-\345\256\211\345\205\250\344\270\216\345\217\257\351\235\240\346\200\247\350\247\204\350\214\203.md"
- M "docs/\351\241\271\347\233\256\347\212\266\346\200\201.md"
- M src/XMacroBridge.App/App.xaml.cs
- M src/XMacroBridge.App/XMacroBridge.App.csproj
- M src/XMacroBridge.Core/Conversion/MacroValidator.cs
- M src/XMacroBridge.Core/Conversion/NestedMacroResolver.cs
- M src/XMacroBridge.Core/Models/MacroEvent.cs
- M src/XMacroBridge.Formats.Razer/RazerMacroXmlExporter.cs
- M src/XMacroBridge.Formats.Xmbc/XmbcMacroTextExporter.cs
- M src/XMacroBridge.Formats.Xmbc/XmbcMacroTextImporter.cs
- M tests/XMacroBridge.Core.Tests/Program.cs
- M tests/XMacroBridge.Core.Tests/XMacroBridge.Core.Tests.csproj
- ?? src/XMacroBridge.Application/Exporting/ExportResult.cs
- ?? src/XMacroBridge.Application/Exporting/SafeExportService.cs
- ?? src/XMacroBridge.Application/Formats/MacroFormatRegistry.cs
- ?? src/XMacroBridge.Application/Importing/ImportBatchResult.cs
- ?? src/XMacroBridge.Application/Importing/MacroImportOptions.cs
- ?? src/XMacroBridge.Application/Importing/MacroImportService.cs
- ?? src/XMacroBridge.Application/XMacroBridge.Application.csproj

### 项目待办快照

- 验证雷云 XML 在当前雷云 4 版本中的实际导入行为。
- 建立 UI 工作区 ViewModel，并接入应用层导入与导出服务。
- 增加批量导入进度、取消和诊断分组的 UI 展示。
- 阶段 1：继续扩充匿名化格式基线。
<!-- AUTO-SUMMARY:END -->
## 开发会话 00:08:49

### 完成事项
- 实现 .synapse4 只读导入器：安全解析外层 JSON、Base64 内层载荷、条目级错误隔离和嵌套引用事件。新增匿名化父子宏整包样本。

### 测试及验证
- Release 构建 0 警告、0 错误；17 项离线测试全部通过，包括整包导入到嵌套展开、损坏 Base64 和非对象事件。

### 技术决策
- .synapse4 hash 在算法未确认前只产生未验证提示，不作为读取失败条件。

### 风险或阻塞
- 整包输出仍不在第一版范围；MPIndex 语义需继续使用真实版本样本验证。

### 后续待办
- 实现 XMBCSettings.xml/.xmbcp 中 action=28 宏提取。
## 开发会话 00:25:52

### 完成事项
- 修复干净仓库日报误报；实现 XMBCSettings.xml/.xmbcp action=28 提取、来源元数据与命名；实现 XMBC 文本和雷云 XML 安全导出。

### 测试及验证
- Release 构建 0 警告、0 错误；23 项 .NET 测试及日报干净仓库回归检查全部通过。

### 技术决策
- 配置导入保留 keyaction 等原始属性，不在数值语义未确认时换算；导出失败不写入目标流。

### 风险或阻塞
- HOLDMS 与组合修饰键的精确语义仍需进一步从 XMBC UI/官方资料确认。

### 后续待办
- 核对 XMBC 完整函数标记；验证导出的雷云 XML 在雷云 4 中实际导入；开始应用层导入编排。
## 开发会话 00:34:33

### 完成事项
- 依据用户提供的 XMBC 软件内说明，重构宏文本语义：辅助键仅作用下一键、HOLD/HOLDMS、连续 PRESS/RELEASE、扩展/功能/多媒体/浏览器/数字键盘与扩展鼠标标记。

### 测试及验证
- Release 构建 0 警告、0 错误；29 项 .NET 测试和日报回归检查全部通过。

### 技术决策
- 辅助键独立状态导出使用 VKC/EXT 配合 PRESS/RELEASE，避免误用仅作用下一键的辅助键标记。

### 风险或阻塞
- 移动、运行程序、Windows 消息和方案切换无法等价转换到雷云，当前继续作为阻断诊断。

### 后续待办
- 为随机延时、扫描码及非键鼠命令建立可保留事件类型；开始应用层文件编排。
## 开发会话 00:44:16

### 完成事项
- 新增随机延时、扫描码和 XMBC 专属命令统一事件；支持 XMBC 原样导回并阻断不兼容雷云输出；建立 Application 层格式注册、文件/目录批量导入与安全原子导出。

### 测试及验证
- 6 个项目 Release 构建 0 警告、0 错误；38 项 .NET 测试和日报回归检查全部通过。

### 技术决策
- XMBC 专属命令作为有效来源事件保留，目标导出器负责能力判断；UI 不直接调用格式或文件系统实现。

### 风险或阻塞
- 雷云实际导入仍需人工在 Synapse 4 中验收；批量导入的超大真实目录还需压力测试。

### 后续待办
- 建立 UI 工作区 ViewModel，接入批量导入、诊断分组、取消和安全导出。
## 开发会话 01:05:22

### 完成事项
- 建立独立 Presentation 层与 WorkspaceViewModel，接入批量导入、宏选择、事件预览、嵌套展开、基础验证、目标格式选择、进度、取消和安全导出。
- 完成首版 WPF 工作区界面：文件/文件夹/拖放导入、宏列表、事件时间线、兼容性报告和安全另存。
- 补充 4 项工作区回归测试，并通过实际启动冒烟检查修复 ProgressPercent 只读属性的 WPF 默认 TwoWay 绑定异常。
- 同步更新系统架构、UI 规范、测试标准和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；42 项 .NET 测试和日报回归检查全部通过。
- 实际启动 Release 主窗口，确认空状态、控件禁用状态和基础布局可正常呈现。
- git diff --check 通过；我的宏目录未进入 Git 跟踪。

### 技术决策
- Presentation 保持 net8.0 且不引用 WPF；MainWindow 代码仅处理 Windows 对话框、拖放和 ViewModel 调用。
- 无效宏在禁用导出的同时立即显示结构化阻断诊断；目标格式专属不兼容仍由导出器最终判断。

### 风险或阻塞
- 当前 UI 仅完成浅色主题基线；深色主题、高对比度和自动化 UI 冒烟仍需继续。
- 雷云 4 实际导入验证尚未完成。

### 后续待办
- 实现诊断按宏、来源和严重级别筛选分组。
- 实现深色主题及 Windows 主题实时切换。
- 在雷云 4 和 XMBC 中完成生成结果的实机导入验收。
## 开发会话 01:18:38

### 完成事项
- 实现诊断严重级别与来源筛选，并按工作区、宏或文件上下文分组显示。
- 实现浅色、深色和高对比度语义主题，只读跟随 Windows 应用主题并实时刷新。
- 使用匿名化 samples 文件夹完成 UI 实跑，成功导入 8 个宏并显示 3 条分组诊断。
- 同步更新架构、UI、安全、测试规范和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；43 项 .NET 测试和日报回归检查全部通过。
- 实际启动 Release 主窗口，验证筛选控件、诊断分组、事件时间线和导出状态。

### 技术决策
- 诊断筛选和分组保持在 Presentation 层，不依赖 WPF CollectionView。
- 主题检测只读访问 Windows 当前用户偏好，不写注册表；所有颜色通过动态语义资源刷新。

### 风险或阻塞
- 深色和高对比度逻辑已实现，但仍需发布级视觉、键盘和屏幕阅读器专项验收。
- 雷云 4 实际导入仍会改变真实软件宏列表，需要在明确授权后执行。

### 后续待办
- 增加 WPF 自动化冒烟测试。
- 完成三种主题的发布级可访问性验收。
- 在获得确认后执行雷云 4 与 XMBC 实机导入验收。
## 开发会话 01:25:57

### 完成事项
- 新增 XMacroBridge.App.SmokeTests，真实运行 WPF Dispatcher 和主窗口并验证关键绑定与控件状态。
- 新增命令行文件/目录只读导入入口，与文件选择、拖放共用 Application 导入管线。
- 将 WPF 冒烟纳入 tools/test.ps1 完整测试门禁。
- 同步更新产品需求、架构、工作流程、测试标准和项目状态。

### 测试及验证
- 8 个项目 Release 构建 0 警告、0 错误；43 项核心测试、WPF 工作区冒烟和日报回归检查全部通过。
- WPF 冒烟导入匿名化 samples，验证 8 个宏、3 条诊断、时间线、筛选器和按钮状态后自动退出。

### 技术决策
- WPF 冒烟在独立 net8.0-windows 测试项目中运行，不引入第三方 UI 自动化依赖。
- 命令行路径只接受当前确实存在的文件或目录，并继续使用既有资源限制与只读策略。

### 风险或阻塞
- WPF 冒烟覆盖启动和绑定，不替代多 DPI、三主题和屏幕阅读器发布验收。

### 后续待办
- 执行雷云 4 与 XMBC 实机导入验收。
- 完成三种主题的发布级可访问性验收。
## 开发会话 01:33:38

### 完成事项
- 完成基础实机兼容性验收：Razer Synapse 4 成功导入匿名化独立宏 XML，并在宏列表显示“匿名化基础宏”。
- XMBC 2.20.5 便携版模拟按键编辑器接受基础 PRESS/RELEASE、WAITMS 和 LMB 文本组合。
- XMBC 测试后恢复鼠标中键为无修改，未点击应用或保存配置。
- 同步更新宏格式规范、转换规则、测试基线和项目状态。

### 测试及验证
- Synapse 4 实际导入 samples/razer/basic-key-delay.xml 成功。
- XMBC 2.20.5 接受 {PRESS}a{WAITMS:50}{RELEASE}a{WAITMS:10}{LMB}。

### 技术决策
- 实机结论仅覆盖基础键盘、固定延时和左键事件，不外推到扩展事件或未来软件版本。

### 风险或阻塞
- Synapse 4 中已新增匿名化测试宏“匿名化基础宏”；未执行删除以避免未经确认的数据删除操作。
- 扩展键、随机延时、滚轮、扫描码和其他鼠标按钮仍需逐项实机验收。

### 后续待办
- 扩充扩展事件匿名化实机样本。
- 完成三种主题的发布级可访问性验收。
## 开发会话 01:49:30

### 完成事项
- 扩展 WPF 冒烟为浅色、深色和高对比度三套真实窗口测试。
- 使用 WCAG 相对亮度公式验证正文、辅助文字、强调色、主按钮及错误/警告/成功色至少达到 4.5:1。
- 为宏列表、时间线、目标格式、诊断筛选/分组、进度和主要按钮补充可访问名称。
- 修正深色错误红和高对比度强调色，并将按钮填充色与普通强调文字色解耦。
- 收紧命令行主题测试参数解析，避免测试参数被误当作导入路径。

### 测试及验证
- 8 个项目 Release 构建 0 警告、0 错误；43 项核心测试、三主题 WPF 可访问性冒烟和日报回归全部通过。
- 浅色、深色和高对比度主题的关键语义色均达到 4.5:1；按钮和下拉框可进入键盘焦点。

### 技术决策
- 主按钮使用独立 AccentFillBrush/AccentFillTextBrush，避免一个强调色同时承担文本和填充两种冲突对比度需求。
- 高对比度主题强调文字和按钮使用 SystemColors.WindowText/Window，选中状态继续使用系统 Highlight。

### 风险或阻塞
- 自动门禁不替代 125%-200% DPI、完整 Tab 顺序和屏幕阅读器人工验收。

### 后续待办
- 完成多 DPI、完整键盘遍历和屏幕阅读器人工验收。
- 扩充扩展事件匿名化实机样本。
## 开发会话 01:55:30

### 完成事项
- 新增 XMBC 扩展标记匿名化黄金样本，覆盖辅助键、随机延时、F24、中键、MB4、滚轮、数字键盘、音量、多媒体、浏览器、扫描码、移动和方案命令。
- 新增扩展样本文件导入、模型验证、XMBC 导出和再次导入的事件签名回归。
- 修复 MWUP/MWDN/TILTL/TILTR 原子标记的配对释放被误判为 XMBC_EXPORT_MOUSE_UNSUPPORTED。
- WPF 三主题冒烟同步扩展为 9 个匿名化宏。
- 同步更新宏格式、转换规则、测试标准和项目状态。

### 测试及验证
- 8 个项目 Release 构建 0 警告、0 错误；44 项核心测试、三主题 WPF 冒烟和日报回归全部通过。
- 扩展 XMBC 黄金样本完成事件级语义往返，滚轮释放不再产生错误。

### 技术决策
- XMBC 滚轮与倾斜标记按原子动作导出：Down 输出标记，配对 Up 只消费不输出。

### 风险或阻塞
- 扩展黄金样本验证结构和语义往返，不代表目标系统实际执行行为已逐项确认。

### 后续待办
- 在 XMBC 2.20.5 中逐项验证扩展样本实际接受与执行边界。
- 继续扩充雷云扩展事件匿名化样本。
## 开发会话 02:48:38

### 完成事项
- 阻断非相邻、错按钮及孤立的 XMBC 滚轮/倾斜原子事件，并补齐 MWUP/MWDN/TILTL/TILTR 导出回归。
- 启用正式窗口 PerMonitorV2；在 900×500 紧凑布局中保持纵向滚动且为列表/时间线提供有限测量空间。
- 按真实页面、次级、交替行、诊断、选中与成功徽章表面校验三主题；修复深色错误色、按钮 pressed 对比度、高对比度配对和键盘焦点环。
- 新增统一 UIA 名称脱敏，覆盖绝对路径、GUID、内部类型和原始事件载荷；测试主题参数仅在自动化环境生效。
- 新增 ADR-0002，并同步宏格式、转换、UI、安全、测试、发布规范和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；47 项核心测试全部通过。
- 浅色、深色和高对比度真实 WPF 冒烟全部通过。
- 5,000 事件时间线已实现 DataGridRow 少于 200，虚拟化门禁通过。
- PerMonitorV2 窗口 context、900×500 无横向滚动、Tab/UIA/live region、日报回归均通过。

### 技术决策
- XMBC 原子滚轮/倾斜只允许相邻同按钮 Down/Up 折叠，其他顺序一律阻断。
- 紧凑工作区高度绑定 ScrollViewer ViewportHeight 并保留最小高度，兼顾整页滚动和内部虚拟化。
- UIA 名称只使用脱敏来源或受控摘要；按钮交互态使用不透明语义色和独立焦点环。

### 风险或阻塞
- 真实 125%–200% 跨屏 DPI、Narrator 语音和正式发布 EXE 嵌入 manifest 尚需后续验收。

### 后续待办
- 隔离雷云损坏事件条目并保留同宏有效事件。
- 补 UTF-8 BOM、UTF-16 LE/BE 输入编码基线。
- 为雷云延时毫秒舍入精度损失产生可见 Warning。
## 开发会话 03:23:32

### 完成事项
- 雷云独立 XML 与 .synapse4 对已知类型损坏事件执行事件级隔离，保留同宏前后有效事件；坏宏条目不阻止同包后续宏。
- 新增 RazerDelayConverter，非整数毫秒产生 RAZER_DELAY_PRECISION_LOSS，负亚毫秒延时不再舍入成合法零。
- 新增严格 BOM 编码检测：四类外层输入支持 UTF-8 BOM、UTF-16 LE/BE BOM；Synapse 内层保持 UTF-8，UTF-32和本地代码页可见拒绝。
- Synapse 路由不再要求 macros 位于 4096-byte 探测窗口内。
- 文件级诊断来源统一为脱敏文件名，UnknownMacroEvent.RawPayload 不再进入 SourceContext。
- 同步更新架构、格式、转换、安全、测试规范和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；56 项核心测试全部通过。
- 浅色、深色和高对比度真实 WPF 冒烟全部通过。
- 编码矩阵、XML 声明门禁、UTF-32 拒绝、Synapse 内层 UTF-8、服务层只读 SHA-256 回归全部通过。
- 日报干净仓库回归通过。

### 技术决策
- UTF-16 只在存在 LE/BE BOM 时接受；不猜测无 BOM UTF-16，导出继续统一 UTF-8 no-BOM。
- XML 在交给 XmlReader 前执行严格全量字节校验和声明/BOM一致性检查。
- 损坏事件占据原序号并转为 UnknownMacroEvent，原始载荷仅留在事件模型中。

### 风险或阻塞
- 各格式解析循环尚未在构造模型前统一执行 100,000 事件上限，已列入待办。

### 后续待办
- 将事件数量上限前移到各格式解析循环并补压力测试。
- 为正式发布 EXE 增加嵌入 manifest 自动检查。
- 完成真实跨屏 DPI 与 Narrator 人工验收。
## 开发会话 03:34:06

### 完成事项
- 向 Razer XML、Synapse、XMBC 文本和 XMBC 配置导入器注入 MacroLimits，并在解析循环内强制 MaximumEventsPerMacro。
- 达到上限时产生 IMPORT_EVENT_LIMIT，并在上限内写入 import.event-limit UnknownMacroEvent 哨兵，确保截断宏无法导出。
- 新增默认 100,000 事件大输入压力回归；四类导入路径同时使用小型注入上限验证。
- 修复 XMBC 未知标记将原始 token 写入 SourceContext 的隐私问题。
- 同步更新架构、转换、安全、测试规范和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；58 项核心测试全部通过。
- 默认 100,000 事件压力输入返回模型严格等于上限，只有一条限流诊断且保持阻断。
- 浅色、深色和高对比度真实 WPF 冒烟全部通过。
- 日报干净仓库回归通过。

### 技术决策
- 事件上限至少为 1；超限时用最后一个槽位保存截断哨兵，而不是返回可被误导出的完整有效前缀。
- XMBC 单标记展开后立即裁剪，最大瞬时超出仅限单个标记的有限展开量。

### 风险或阻塞
- XML/JSON 仍先由受 64 MiB 文件上限保护的 DOM 解析器读取结构；事件模型本身现已在循环内受限。

### 后续待办
- 为正式发布 EXE 增加嵌入 manifest 自动检查。
- 完成真实跨屏 DPI 与 Narrator 人工验收。
- 继续扩充雷云与 XMBC 匿名化事件基线。
## 开发会话 03:43:01

### 完成事项
- 新增可复用的发布 EXE RT_MANIFEST 只读校验器，直接检查默认资源中的 PerMonitorV2,PerMonitor。
- 将 Windows x64 自包含临时发布和 Manifest 校验接入 tools/test.ps1，验证结束自动清理系统临时目录。
- 同步测试验收、构建发布规范、测试索引和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；58/58 核心测试通过。
- Windows x64 自包含 XMacroBridge.App.exe 成功生成，嵌入 RT_MANIFEST PerMonitorV2 检查通过。
- 校验器已确认会拒绝仅声明 PerMonitorV2、缺少项目兼容回退值的非符合 EXE。
- 浅色、深色、高对比度 WPF 冒烟及日报干净仓库回归全部通过。

### 技术决策
- 发布 DPI 门禁必须读取真实自包含 EXE 的 RT_MANIFEST 资源；只检查源 app.manifest 或运行时窗口状态均不能替代。
- 校验器使用 LoadLibraryEx 只读资源模式，不执行被检查 EXE 的程序代码。

### 风险或阻塞
- 真实 125%–200% 跨显示器切换和 Narrator 语音仍需人工验收。

### 后续待办
- 完成真实 125%–200% 跨显示器拖动、完整 Narrator 播报和物理屏幕人工验收。
- 继续扩充雷云与 XMBC 匿名化事件格式基线。
## 开发会话 04:19:24

### 完成事项
- 实现时间线固定延时修改，以及固定延时和随机延时的批量百分比缩放。
- 编辑使用新的只读宏事件快照替换工作区对象，保留原导入对象与原文件；建立最多 20 步撤销/重做，分叉编辑清空重做。
- 每次编辑、撤销和重做后即时重新解析与验证，并替换工作区动态诊断，已修复错误不会残留阻断。
- 新增时间线编辑工具栏、选中事件绑定、窄宽度自动换行、完整 Tab 顺序和可访问名称。
- 修复重复宏 GUID 时可能替换错误条目的问题：优先按对象引用定位，仅在 GUID 唯一时回退。
- 同步架构、转换、UI、安全、测试规范和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；63/63 核心测试通过。
- 覆盖原对象不变、AwayFromZero 缩放、随机延时双边界、溢出与无效区间整批拒绝、即时诊断、撤销/重做、分叉历史和 20 步上限。
- 重复宏 GUID 回归先复现改错条目，再验证对象引用隔离修复通过。
- 浅色、深色和高对比度真实 WPF 冒烟、900×500 紧凑布局、编辑绑定、Tab/UIA、发布 Manifest 和日报回归全部通过。

### 技术决策
- 时间线编辑只替换工作区中的不可变宏快照，不修改导入对象；历史上限固定为 20 步以控制超长宏内存占用。
- 批量缩放先计算全部固定和随机延时，使用 AwayFromZero 舍入；任一无效值或溢出时整批拒绝。
- 工作区宏替换优先使用对象引用，只有 GUID 唯一时才允许标识回退。

### 风险或阻塞
- 阶段 5 的事件插入、删除、复制、移动和搜索尚未实现。
- 真实 125%–200% 跨显示器切换和 Narrator 语音仍需人工验收。

### 后续待办
- 实现时间线事件插入、删除、复制和移动，并复用当前撤销/重做与即时验证基础。
- 实现事件搜索与定位。
- 继续扩充雷云与 XMBC 匿名化事件格式基线。
## 开发会话 04:36:01

### 完成事项
- 新增 TimelineEditOperations，以用户可见顺序执行固定延时插入、任意事件复制、删除和上下移动，并统一生成连续 Sequence。
- 扩展编辑历史，分别保存编辑前和编辑后选择位置，使插入、删除、复制和移动在撤销/重做时恢复正确选中项。
- 结构编辑接入 MaximumEventsPerMacro；插入与复制达到上限时拒绝且不创建历史。
- 复制、删除和移动后即时重新验证；危险按键顺序禁用导出，撤销后同步恢复诊断与导出状态。
- 新增两行时间线编辑工具栏、完整 Tab 顺序、可访问名称和真实按钮绑定冒烟。
- 语义无变化的相同事件交换不进入历史，并通过状态 live region 给出提示。
- 同步架构、转换、UI、安全、测试规范和项目状态。

### 测试及验证
- Release 构建 0 警告、0 错误；67/67 核心测试通过。
- 覆盖选中后插入、无选择追加、删除唯一事件、复制危险键盘事件、上下移动、连续序号、边界/无变化操作、事件上限和选择恢复。
- 浅色、深色和高对比度真实 WPF 冒烟验证结构编辑按钮、绑定、Tab/UIA、900×500 布局和时间线虚拟化。
- 自包含发布 Manifest 与日报干净仓库回归通过。

### 技术决策
- 结构编辑先按 Sequence 和原索引形成稳定可见顺序，操作完成后统一重新编号为连续序号。
- 当前安全插入入口只新增固定延时；键盘和鼠标事件将在参数化输入完成后开放，避免猜测键码和按下/释放语义。
- 结构编辑允许产生可见的安全诊断，但绝不自动补键、调整其他事件或静默保持可导出。

### 风险或阻塞
- 键盘和鼠标事件的参数化插入尚未实现。
- 事件搜索与定位尚未实现。
- 真实 125%–200% 跨显示器切换和 Narrator 语音仍需人工验收。

### 后续待办
- 实现键盘与鼠标事件参数化插入，并验证按下/释放状态。
- 实现事件搜索、下一项/上一项定位和筛选结果计数。
- 继续扩充雷云与 XMBC 匿名化事件格式基线。
