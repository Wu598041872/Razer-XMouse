# 开发日志：2026-07-13

## 当日目标

- 待填写。

## 已完成事项

- 待填写。

## 测试及验证

- 待填写。

## 技术决策

- 无。

## 风险、问题和阻塞

- 无。

## 未完成事项

- 待填写。

## 下一开发日待办

- 待填写。

<!-- AUTO-SUMMARY:START -->
## 自动汇总

尚未生成。
<!-- AUTO-SUMMARY:END -->
## 开发会话 00:58:54

### 完成事项
- 主界面键盘事件输入框改为直接按键捕获并显示可读名称，后台继续保存真实 Windows VK。
- 键盘事件编辑弹窗支持直接按 A、Alt 等按键，自动更新扩展键状态。
- 编辑弹窗按内容自适应高度，修复扩展键选项与底部操作区显示不完整。
- 生成 XMacroBridge-1.0.2-win-x64.zip 与 SHA-256。

### 测试及验证
- Release 构建 0 警告、0 错误。
- 100/100 核心测试通过。
- 浅色、深色、高对比度真实 WPF 冒烟、PerMonitorV2 Manifest、绿色包版本、内容和 SHA-256 校验全部通过。
- 验证 E 显示为 E/VK 69，左 Alt 显示为左 Alt/VK 164，右 Ctrl 显示为右 Ctrl/VK 163。

### 技术决策
- 界面仅显示可读按键名称，数字 VK 保留为内部数据和导出编码。

### 风险或阻塞
- 独立干净 Windows 10/11 环境的最终人工验收仍待完成。

### 后续待办
- 在独立干净 Windows 10/11 x64 环境验收 1.0.2。
## 开发会话 01:23:34

### 完成事项
- 修复中文输入法启用时部分真实按键被误识别为 VK 229。
- 统一处理 IME Processed、System 与 Dead Char Processed 包装事件，主界面和编辑弹窗共用捕获逻辑。
- 补齐常见 OEM 标点、输入法、媒体和特殊功能键的可读名称，未知按键不再显示数字 VK。
- 生成 XMacroBridge-1.0.3-win-x64.zip 与 SHA-256。

### 测试及验证
- Release 构建 0 警告、0 错误。
- 100/100 核心测试通过。
- 浅色与深色真实 WPF 冒烟通过。
- PerMonitorV2 Manifest、绿色包版本、内容和 SHA-256 校验通过。

### 技术决策
- 先还原 WPF 包装事件中的底层真实按键，再计算 VK 和显示名称。

### 风险或阻塞
- 本轮高对比度 WPF 冒烟在退出阶段出现环境性挂起，未报告功能断言错误，需后续复查。

### 后续待办
- 在用户真实中文输入法环境复测此前显示 VK 229 的按键。
## 开发会话 01:41:03

### 完成事项
- 修复目标格式下拉框 SelectedValuePath 错误，选择 X-Mouse 宏文本后后台同步为 xmbc.macro.text。
- 安全导出前再次同步当前下拉选项，保存对话框使用 X-Mouse 文本类型、.txt 扩展名和建议文件名。
- 删除 ThemeManager、ThemeMode、系统主题监听和测试主题覆盖参数，只保留固定 Razer 暗色资源。
- 将 WPF 冒烟由三主题重复启动精简为一次固定风格测试。
- 生成 XMacroBridge-1.0.4-win-x64.zip 与 SHA-256。

### 测试及验证
- Release 构建 0 警告、0 错误。
- 100/100 核心测试通过。
- 固定 Razer 风格真实 WPF 冒烟通过，验证 X-Mouse 选择同步为 .txt。
- PerMonitorV2 Manifest、绿色包版本、内容和 SHA-256 校验通过。

### 技术决策
- 保持自包含绿色包；固定主题代码删除只减少少量体积，主要包体来自 .NET 8 WPF 自包含运行时。

### 风险或阻塞
- 自包含 WPF 运行时仍占发布包主要体积；若继续显著缩小需改为依赖用户安装 .NET Desktop Runtime。

### 后续待办
- 在真实保存对话框复测 XML 转 X-Mouse 时默认文件类型和扩展名为 .txt。

## 开发会话 02:10:00

### 完成事项
- 对照四份用户独立 XML，确认两个父宏的 Type 7 引用 GUID 均与对应子宏根 `<Guid>` 精确一致。
- 定位独立 XML 导入器忽略根 Guid、改用文件哈希 GUID 的根因，修复为有效根 Guid 优先，缺失或无效时才回退内容哈希。
- 修复 `MPIndex` 的基数错误：雷云值从 1 开始，内部列表访问改为 `index - 1`，消除 `MPIndex=1` 错误指向父宏自身造成的循环引用。
- 新增独立 XML 根 Guid 跨文件解析回归，并将索引回退测试改为一基语义。
- 使用用户四份真实 XML完成只读验证；“火茜砂尼8融冷启动”成功展开为 229 个执行事件，并生成无 Type 7 的展开版 XML。

### 测试及验证
- 修复前回归稳定复现根 Guid 丢失与一基 MPIndex 解析失败；修复后 101/101 核心测试、Release 构建、固定 Razer 风格 WPF 冒烟、PerMonitorV2 Manifest 和日报回归通过。
- 真实文件导入得到 GUID：252a04f0-d244-421f-ad81-463f04f4a8a1、b9639fca-5487-467a-b19c-79c1890d500f、86576658-acf9-4eb7-8205-ed440a634f06、2d9ecb50-4ef5-416e-97ab-ab0a90a682bd，与 XML 根值一致。
- “火茜砂尼8融冷启动-展开.xml”包含 230 个 XML 事件（含 actionBar）、0 个 Type 7 引用。
- 版本提升为 1.0.5，绿色包版本、内容、Manifest 和 SHA-256 校验通过；SHA-256 为 `592CE31A4216B054FBD9AD230DECDE0C2E2F0CEF53AD1113645B26A8E3159EE4`。

### 技术决策
- 文件内容哈希 GUID 只用于没有可靠格式内标识的输入，不能覆盖格式自身提供的真实引用键。
- MPIndex 仅作为 GUID 未命中时的兼容回退，且保持雷云的一基位置语义。

### 风险或阻塞
- “火茜砂尼小轴.xml”末尾两个 Type 2 事件的 MouseButton 为空，按损坏事件保留为 Unknown 并阻断安全导出；嵌套 GUID 已能解析，但展开版不能在不丢弃事件的情况下安全生成。

### 后续待办
- 用户确认末尾两个空 MouseButton 事件是否为无效占位；确认后可在界面删除，或由用户授权生成移除这两个损坏事件的展开版。
## 开发会话 02:26:53

### 完成事项
- 新增嵌套宏目标下拉框，列出当前工作区除当前宏外的已导入宏。
- 支持将选中的 MacroReferenceEvent 手动绑定到目标宏，并支持在选中位置后插入新嵌套引用。
- 手动绑定写入 GUID、名称和一基工作区位置，进入撤销/重做并即时刷新缺失引用、循环引用和导出诊断。
- 版本提升为 1.0.6 并生成 Windows x64 绿色包。

### 测试及验证
- 102/102 核心测试通过。
- Release 全解决方案构建 0 警告、0 错误。
- 真实本地 WPF 窗口确认嵌套宏标签、下拉框、绑定宏与插入宏控件布局及可访问名称正常。
- PerMonitorV2 Manifest、1.0.6 版本、发布包内容和 SHA-256 校验通过。

### 技术决策
- 自动 GUID/MPIndex 匹配继续保留；手动选择作为用户可控覆盖，不修改原始文件。
- 当前宏从目标列表排除；间接循环继续由 NestedMacroResolver 统一阻断。

### 风险或阻塞
- 自动 WPF 冒烟在退出阶段再次出现已知环境性挂起，未产生功能断言失败；本轮以核心回归、Release 构建和真实窗口检查覆盖新增功能。

### 后续待办
- 用户使用 1.0.6 重新导入父宏和目标宏，选中缺失嵌套事件后从下拉框手动绑定并验证导出。
## 开发会话 03:01:05

### 完成事项
- 修复通用 ComboBox 模板只显示 Text 导致对象选项收起后为空白的问题，改用 SelectionBoxItem、模板和格式呈现。
- 嵌套目标选择区加宽至 240 DIP，新增未选择占位、长名称省略和完整名称悬停提示，标签改为嵌套目标。
- 版本提升为 1.0.7 并生成 Windows x64 绿色包。

### 测试及验证
- 102/102 核心测试通过。
- Release 全解决方案构建 0 警告、0 错误。
- PerMonitorV2 Manifest、1.0.7 版本、发布包内容和 SHA-256 校验通过。

### 技术决策
- 在通用 ComboBox 模板修复 SelectionBoxItem，而不是仅为嵌套宏硬编码显示文本，确保对象型选项均能正确呈现。

### 风险或阻塞
- 前台可视复查时检测到用户正在操作窗口，已停止继续自动交互以避免抢占焦点；WPF 控件回归已编译，完整自动冒烟仍有既有退出挂起风险。

### 后续待办
- 用户使用 1.0.7 验证选择嵌套目标后收起的输入区域显示宏名。
## 开发会话 03:09:57

### 完成事项
- 根据用户截图完整回退 1.0.7 对全局 ComboBox 模板的 SelectionBoxItem 修改。
- 只在 NestedMacroTargetSelector 上增加 SelectedReferenceTarget.Name 到 Text 的单向绑定，保留 240 DIP 宽度和完整名称提示。
- 新增普通下拉框 DisplayName 与嵌套目标名称显示回归断言。
- 版本提升为 1.0.8 并生成 Windows x64 绿色包。

### 测试及验证
- git diff 确认 Themes/Controls.xaml 全局样式已恢复且无差异。
- 102/102 核心测试通过。
- Release 全解决方案构建 0 警告、0 错误。
- PerMonitorV2 Manifest、1.0.8 版本、发布包内容和 SHA-256 校验通过。

### 技术决策
- 对象名称显示修复必须局部作用于嵌套目标，禁止改变全局 ComboBox 模板。

### 风险或阻塞
- 完整 WPF 自动冒烟仍有既有退出阶段环境性挂起；本轮通过全局样式零差异、XAML 局部绑定回归、构建和核心测试验证回退范围。

### 后续待办
- 用户使用 1.0.8 确认普通下拉框恢复正常，嵌套目标选择后显示宏名。

## 开发会话：全界面 UI/UX 精修

### 完成事项
- 新增工作区、时间线、诊断与宏库的只读界面状态，按无宏、已选宏、未选事件和多选事件分阶段显示工具。
- 时间线增加双入口空状态与空宏引导；报告增加通过检查、筛选无结果和重置筛选状态，并将进度与取消移入独立状态栏。
- 宏库增加两类列表空状态、稳定两层操作区、准确的分组/回收站启用条件和非阻塞忙碌状态。
- 建立统一搜索框、只读字段、危险按钮、空状态、等宽数字和语义表面资源；移除视图层硬编码颜色。
- 统一暗色应用确认框和编辑弹窗标题栏，删除/永久删除/清空操作使用危险层级；事件校验增加图标、文字、UIA live region 与首错聚焦。
- 增加 `Ctrl+O`、`Ctrl+F`、`Ctrl+Z`、`Ctrl+Y`，并保留文本框原生撤销/重做；事件搜索支持 Enter、Shift+Enter 和 Esc。
- 设置页显示应用版本与第三方许可入口，装饰性品牌图标不参与点击和焦点。

### 技术决策
- 延续固定 Razer 暗色工业控制台风格和 Tabler 矢量图标，不引入第三方 UI 框架或复杂动画。
- 交互反馈只改变颜色与预留边框，不改变控件尺寸或边框占位，避免悬停和选中造成布局抖动。
- 原生文件选择、文件夹选择和保存窗口继续使用 Windows 实现；应用自有提示与确认全部使用统一暗色弹窗。

### 测试及验证
- `tools/test.ps1 -Configuration Release` 全部通过：102/102 核心测试、Release 构建 0 警告 0 错误、自包含发布、PerMonitorV2 清单、WPF 冒烟和日报回归。
- 新增视图硬编码颜色门禁，除主题字典和品牌资源外未发现散落颜色值。
- 实际启动检查默认 `1265 × 813` 和紧凑 `900 × 500`；宏转换、宏库、设置页面无横向溢出，紧凑高度使用纵向滚动。
- 可视检查发现并修复无宏空状态误入标题行的问题，引导区现位于时间线编辑区域中央。
- 生成并实际启动自包含测试版本 `XMacroBridge-test-ui-polish-20260713-042041`。
## 开发会话 11:25:06

### 完成事项
- 宏库 XMouse 与雷蛇 XML 列表新增拖拽排序和绿色插入位置提示。
- 自定义顺序持久化到宏库 metadata.json，并随重命名和分组移动更新。
- 工作区导入由清空替换改为追加，新导入宏自动选中，已有宏继续保留。
- 搜索、回收站和按时间排序状态下禁用手动拖拽以避免规则冲突。
- 生成 XMacroBridge-1.0.9-win-x64.zip 与 SHA-256。

### 测试及验证
- Release 构建 0 警告、0 错误。
- 103/103 核心测试通过，覆盖二次导入保留首个宏与宏库顺序持久化。
- 固定 Razer 风格 WPF 冒烟通过，验证两个宏库列表允许拖拽。
- PerMonitorV2 Manifest、版本、包内容和 SHA-256 校验通过。

### 技术决策
- 宏库手动顺序使用相对路径列表保存；新条目默认追加到未排序条目末尾。

### 风险或阻塞
- 搜索、回收站及按时间排序视图不启用手动拖拽。

### 后续待办
- 在真实宏库中拖动多条 XMouse/XML 宏并重启确认顺序。
## 开发会话 12:30:19

### 完成事项
- 将当前 1.0.9 最新源码升级为 1.1.0 功能汇总版本。
- 更新 README、发布说明、绿色包说明、项目状态和路线图中的版本信息。
- 生成 XMacroBridge-1.1.0-win-x64.zip 与 SHA-256。

### 测试及验证
- 语义颜色门禁通过，Release 构建 0 警告、0 错误。
- 103/103 核心测试与固定 Razer 风格 WPF 冒烟通过。
- PerMonitorV2 Manifest、ProductVersion 1.1.0、发布包内容和 SHA-256 解包校验通过。
- 日报干净仓库回归通过。

### 技术决策
- 1.1.0 作为 1.0 系列功能汇总版，包含宏库排序、追加导入、嵌套宏编辑、按键捕获和 UI/UX 精修。
- 完整发布脚本的 WPF 退出阶段发生环境性挂起，改用有 60 秒进程边界的分阶段等价门禁后执行 SkipTests 装包。

### 风险或阻塞
- 独立干净 Windows 10/11 最终人工验收仍待完成。

### 后续待办
- 清理根目录旧版本发布物、测试目录和 bin/obj，仅保留 1.1.0 正式包。
## 开发会话 12:32:11

### 完成事项
- 清理 artifacts 中 1.0.7–1.0.9 旧发布物、三个历史测试目录和测试 ZIP。
- 删除本轮生成的全部 src/tests bin/obj，根目录仅保留 1.1.0 正式包及校验文件。
- 本轮清理释放约 0.83 GiB。

### 测试及验证
- 复查 artifacts 仅包含 XMacroBridge-1.1.0-win-x64.zip 与同名 SHA-256。
- 复查项目中不存在 bin/obj、PDB、TMP、BAK 或 IDE 缓存残留。
- 重新计算 1.1.0 ZIP SHA-256 并与校验文件比较。

### 技术决策
- 旧版和测试发布物均可重建，不保留在精简后的根目录。

### 风险或阻塞
- 清理后首次重新构建会恢复依赖并重新生成 bin/obj，属于预期行为。

### 后续待办
- 无。

## 开发会话 15:28:25

### 完成事项
- 已将 XMacro Bridge 1.1.0 绿色版发布到 GitHub Releases，并上传 ZIP 与 SHA-256 校验文件。
- 已创建并推送 v1.1.0 标签，更新项目状态文档。

### 测试及验证
- 复核 GitHub Release 为正式发布；ZIP 附件大小为 70,189,038 bytes，GitHub digest 与本地 SHA-256 849613D4AA09DCB98F1B5A65A9262F4728D8496C147A1EB59BC88B0B9C8F67A5 一致。

### 技术决策
- 发布页标题使用 XMacro Bridge 1.1.0 绿色版；不上传 artifacts 目录及用户的“我的宏”数据。

### 风险或阻塞
- 无。

### 后续待办
- 暂无。
