# 项目状态

最后更新：2026-07-11

## 待办

- [ ] 安装并锁定 .NET 8 SDK，创建 `global.json`。
- [ ] 建立测试项目并选择不依赖网络的初始测试策略。
- [ ] 开始阶段 1：匿名化样本与格式基线。
- [ ] 验证雷云 XML 在当前雷云 4 版本中的实际导入行为。
- [ ] 完整核对 XMBC 官方模拟按键标记表。

## 进行中

- 暂无。

## 已完成

- [x] 确认产品第一版范围与输出边界。
- [x] 建立项目文档标准和工作指引。
- [x] 建立 .NET 8 WPF/Core 基础工程文件。
- [x] 完成阶段 0 工程治理结构。
- [x] 完成日报脚本的语法、幂等、会话记录和任务计划实跑验证。
- [x] 注册 `XMacroBridge-DailyDevLog`，每天 23:50 自动汇总并在错过后补运行。

## 阻塞

- 本机仅有 .NET 8 Runtime，没有 .NET SDK，当前无法执行 `dotnet build` 或 `dotnet test`。

## 风险

- `.synapse4` 的 `hash` 算法尚未确认，因此第一版只读不写整包。
- 雷云和 XMBC 存在未覆盖事件类型，必须通过匿名化样本逐步扩充。
- 用户宏可能包含隐私或游戏操作策略，测试样本必须最小化和匿名化。
