# 项目状态

最后更新：2026-07-12

## 待办

- [ ] 完成真实 125%–200% 跨显示器拖动、完整 Narrator 播报和物理屏幕人工验收。
- [ ] 雷云损坏事件条目隔离：保留宏与有效事件，将损坏项转为 `UnknownMacroEvent` 并产生定位诊断。
- [ ] 建立 UTF-8 BOM、UTF-16 LE 和 UTF-16 BE 的雷云/XMBC 输入编码基线。
- [ ] 雷云秒制延时转换发生毫秒精度损失时产生可见 `Warning`。
- [ ] 发布流水线增加正式 App 自包含产物的嵌入 manifest 自动检查。

## 进行中

- [ ] 阶段 1：继续扩充匿名化格式基线。

## 已完成

- [x] 确认产品第一版范围与输出边界。
- [x] 建立项目文档标准和工作指引。
- [x] 建立 .NET 8 WPF/Core 基础工程文件。
- [x] 完成阶段 0 工程治理结构。
- [x] 完成日报脚本的语法、幂等、会话记录和任务计划实跑验证。
- [x] 注册 `XMacroBridge-DailyDevLog`，每天 23:50 自动汇总并在错过后补运行。
- [x] 安装并锁定 .NET SDK 8.0.422，创建 `global.json`。
- [x] 建立零第三方依赖的离线测试工具和 Core 公共模型、接口、验证器。
- [x] 建立雷云 XML 与 XMBC 宏文本的最小匿名化样本和安全导入基线。
- [x] Release 构建达到 0 警告、0 错误，9 项测试全部通过。
- [x] 实现嵌套宏 GUID 优先/索引回退、缺失引用、循环引用和事件上限验证。
- [x] 实现 `.synapse4` 外层 JSON、Base64 宏载荷和嵌套引用只读导入。
- [x] 建立 XMBC 配置匿名化样本，并提取默认/应用/层级/和弦中的 `action=28` 宏。
- [x] 实现 XMBC 宏文本安全导出和事件级往返测试。
- [x] 实现雷云独立宏 XML 安全导出、事件 ID 配对和事件级往返测试。
- [x] 根据 XMBC 软件内说明实现辅助键、HOLD、连续 PRESS/RELEASE、扩展键和扩展鼠标标记。
- [x] 为随机延时、扫描码和 XMBC 专属命令建立可保留事件，支持 XMBC 原样导回并阻断不兼容雷云输出。
- [x] 实现应用层格式注册、文件/目录批量导入、资源限制和安全原子导出事务。
- [x] 建立独立 Presentation 层和工作区 ViewModel，接入多格式批量导入、宏选择、嵌套展开、验证和安全导出。
- [x] 完成首版 WPF 工作区：文件/文件夹/拖放导入、宏列表、事件时间线、兼容性报告、目标格式、进度与取消。
- [x] 完成 Release 主窗口实际启动冒烟检查，并修复只读进度属性的 WPF 绑定异常。
- [x] 实现诊断按严重级别和宏/文件来源筛选，并按上下文分组显示。
- [x] 实现浅色、深色和高对比度语义主题，实时只读跟随 Windows 系统偏好。
- [x] 使用匿名化样本完成工作区实跑：9 个宏、3 条分组诊断和事件时间线均正常显示。
- [x] 建立 WPF 自动化冒烟测试，真实启动窗口并验证命令行样本导入、关键绑定和控件状态。
- [x] 完成基础实机验收：Synapse 4 接受匿名化独立宏 XML，XMBC 2.20.5 接受基础宏文本且未保存测试配置。
- [x] 建立浅色、深色和高对比度自动可访问性门禁，关键语义色达到 4.5:1，主要控件具备可访问名称。
- [x] 建立 XMBC 扩展标记黄金样本，覆盖随机延时、扩展键鼠、滚轮、扫描码和专属命令的语义往返。
- [x] 修复 XMBC 滚轮/倾斜原子标记的配对释放被误判为不支持的问题。
- [x] 阻断非相邻 XMBC 滚轮/倾斜配对，避免导出静默改变延时与事件顺序。
- [x] 启用 PerMonitorV2，建立 `900 × 500 DIP` 纵向滚动布局合同并消除整页横向滚动。
- [x] 建立复合控件单一 Tab 入口、统一脱敏 UIA 名称和状态 live region 自动门禁，覆盖绝对路径、GUID、内部类型及原始事件载荷。
- [x] 将测试主题参数限制在测试环境，并将 WPF 冒烟固定到独立匿名化文件清单。
- [x] 修复紧凑布局导致时间线失去虚拟化的风险，5,000 事件下已实现行数保持在 200 以下。
- [x] 按真实诊断表面验证三主题颜色，修复深色错误色、按钮 pressed 对比度、高对比度配对和键盘焦点环。
- [x] 将 PerMonitorV2 门禁提升为正式窗口 DPI context 检查，并验证测试主题参数在非测试环境无效。

## 阻塞

- 暂无。

## 风险

- `.synapse4` 的 `hash` 算法尚未确认，因此第一版只读不写整包。
- 雷云和 XMBC 存在未覆盖事件类型，必须通过匿名化样本逐步扩充。
- 当前实机验收只覆盖基础键盘、固定延时和左键序列，不能代表扩展事件或未来软件版本。
- 扩展 XMBC 黄金样本已通过自动化语义往返，但尚未逐项验证实际执行效果。
- 用户宏可能包含隐私或游戏操作策略，测试样本必须最小化和匿名化。
- 三主题实际表面、按钮状态和可访问名称检查已自动覆盖，但多 DPI、完整键盘顺序和屏幕阅读器仍需人工验收。
- PerMonitorV2、紧凑布局、时间线虚拟化和结构化键盘/UIA 合同已自动验证；正式发布 EXE 的嵌入 manifest、真实跨屏 DPI 切换和 Narrator 语音仍需补充验收。
