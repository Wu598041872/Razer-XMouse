namespace XMacroBridge.Core;

/// <summary>
/// Marks the core assembly while format and conversion components are introduced in later stages.
/// </summary>
public static class ProjectMarker
{
    public const string ProductName = "XMacro Bridge";
}
